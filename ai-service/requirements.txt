# ============================================
# 云心伴 - AI 服务依赖
# Python ≥ 3.10
# ============================================

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# 异步
httpx==0.27.0
aiohttp==3.9.5

# LLM Provider
dashscope>=1.20.0          # 通义千问 SDK
openai>=1.30.0              # OpenAI 兼容接口（备用）

# 情感分析
# transformers>=4.41.0      # HuggingFace Transformers（本地模型）
# torch>=2.3.0
# onnxruntime>=1.18.0       # ONNX 推理（轻量部署）

# 文本处理
jieba>=0.42.1               # 中文分词
text2emotion>=0.1.0         # 情感词典

# 数据库
motor>=3.5.1                # MongoDB 异步驱动
asyncpg>=0.29.0             # PostgreSQL 异步驱动
redis>=5.0.0                # Redis 客户端

# 安全
cryptography>=42.0.0        # AES 加密
python-jose[cryptography]>=3.3.0  # JWT

# 工具
python-dotenv>=1.0.1        # 环境变量
loguru>=0.7.2               # 日志
tenacity>=8.3.0             # 重试机制

# 测试
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0               # FastAPI TestClient 依赖
